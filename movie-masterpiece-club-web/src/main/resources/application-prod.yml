spring:
  datasource:
    url: jdbc:mysql://localhost:3306/masterpiece_club?useSSL=false&allowPublicKeyRetrieval=true&useTimezone=true&serverTimezone=Asia/Seoul
    username: root
    password: rootpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: validate
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 763371632947-j0qd1ij5jqivvf5nq5q1p3dd1ss7u6ku.apps.googleusercontent.com
            client-secret: FpBYQ2-dUuZjg1vfIs4iZRNA
            redirect-uri: http://localhost:8080/gulagbu/login/oauth2/callback/google
            scope:
              - email
              - profile

redirect-url: /masterpiece

## context path 가 있기 때문에 google redirect-uri 도 그에 따라 변경됨.
server:
  port: 8080
  servlet:
    context-path: /gulagbu