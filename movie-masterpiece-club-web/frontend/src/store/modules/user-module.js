
import request from '@/request';

const state = {};

const getters = {};

const actions = {

};

const mutations = {

    setCurrentUser(state, params) {
        console.debug(params);
    }
};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}